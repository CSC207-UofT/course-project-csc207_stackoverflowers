package ControllersPresenters;


import UseCases.MonthMaker;

public class MonthLevel {
    //TODO: constructor takes no parameters, will make a new MonthMaker and store it as a private instance.
    private MonthMaker currentMonthMaker;
    private String playerInput;

    //TODO: method getOutput
    // takes in the player's input and then uses the needed method to be used for the output, then asks MonthPresenter to use those stuff for a formatted output

    //TODO: methods formotOutput() takes in a input passes it to MonthMaker, then passes that result to MonthPresenter for formatted output (three for now):
    //getStartOfMonthPrompt()
    //checkProjectInfo()
    //checkInternInfo()

}
