Class name: ReportMaker

Parent class:

Subclasses: 

Responsibilities:
* an interface class that shows all common methods used to make reports:
  * makeReportHeader()
  * makeReportIntro()
  * makeReportBody()
  * endReport()

Collaborators:

