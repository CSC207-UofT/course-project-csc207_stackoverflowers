Class name: ReportMaker

Parent class:

Subclasses: MonthReportMaker, ProjectReportMaker

Responsibilities:
* an interface class that shows all common methods used to make reports:
  * makeReportHeader()
  * makeReportIntro()
  * makeReportBody()
  * endReport()

Collaborators:

