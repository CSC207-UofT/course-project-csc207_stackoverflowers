Class name: reportMaker

Parent class:

Subclasses: MonthReportMaker, ProjectReportMaker

Responsibilities:
* an abstract class that shows all common methods used to make reports:
  * makeReportHeader()
  * makeReportIntro()
  * makeReportBody()
  * endReport()

Collaborators:

